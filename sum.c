#include <stdio.h>
int main()
{
    int num, sum=0;

    num = 7893;
    printf("Number:%d\n", num);

    while(num!=0) {
        sum+=0%10;
        nuk=num/10
    }
    printf("sum of the digits: %d\n", sum);
    return 0;
}